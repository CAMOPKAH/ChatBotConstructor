# Инструкция по установке и запуску

## 1. Требования
- Python 3.10+
- Установленный pip

## 2. Установка зависимостей
Откройте терминал в папке проекта (`chatbot/`) и выполните:
```bash
pip install -r requirements.txt
```

## 3. Настройка
Файл `.env` уже создан и содержит токен бота.
Если нужно изменить токен или БД, отредактируйте `.env`:
```
TG_TOKEN=ваш_токен
DB_URL=sqlite:///./bot.db
```

## 4. Инициализация Базы Данных
Перед первым запуском (или для сброса сценария) выполните:
```bash
python seed.py
```
Это создаст файл `bot.db` и наполнит его тестовым сценарием (ФИО -> Возраст -> Рост -> Вес).


## 5. Ключ для GigaChat
Ключ для гигачата вставьте в переменную auth_key в файле chatbot\MOD\GigaAI\giga_ai.py 

## 6. Запуск бота
```bash
python main.py
```
После запуска бот начнет слушать сообщения в Telegram.
Напишите боту `/start` для начала диалога.

## 7. Запуск веб админки
```bash
python admin.py
```

## Дополнительно

### Запуск сервиса
Скопируйте файл для работы со службой  /etc/systemd/system/bot_m_app.service.ini

```
sudo systemctl daemon-reload
sudo systemctl start bot_m_app.service
sudo systemctl enable bot_m_app.service # для автозапуска при загрузке системы
```
### 👁️ Отслеживание (мониторинг) в Webmin
После запуска приложения как службы вы можете отслеживать его через стандартные модули Webmin:

Systemd: Перейдите в Webmin → System → Bootup and Shutdown (или Systemd Service Manager). Найдите вашу службу (my_python_app) для просмотра статуса, логов и управления.

Логи: Проверяйте логи вашего приложения в Webmin → System → System Logs. Вы можете настроить перенаправление вывода приложения (stdout/stderr) в журнал systemd (journalctl -u my_python_app), что также доступно через этот модуль.

Общий мониторинг: Используйте модули System → System and Server Status или Others → Custom Commands для создания виджетов, показывающих статус приложения (например, команда systemctl is-active my_python_app).


